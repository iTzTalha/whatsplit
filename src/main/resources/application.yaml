spring:
  application:
    name: whatsplit

  datasource:
    url: jdbc:postgresql://${DB_HOST}/${DB_NAME}?sslmode=require&channel_binding=require
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info